import { ImportanceLevelPipe } from './importance-level.pipe';

describe('ImportanceLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ImportanceLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
